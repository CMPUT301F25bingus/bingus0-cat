<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Top Content (Scrollable) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:padding="32dp"
            android:paddingBottom="280dp">

            <!-- App Icon Section with Ticket -->
            <FrameLayout
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_marginTop="80dp"
                android:layout_marginBottom="24dp">

                <!-- Main Ticket Icon (Teal Background) with slight tilt -->
                <com.google.android.material.card.MaterialCardView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:rotation="-8"
                    app:cardCornerRadius="28dp"
                    app:cardElevation="12dp"
                    app:cardBackgroundColor="#5FB3AD">

                    <ImageView
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:src="@drawable/ic_ticket_outline"
                        android:padding="20dp"
                        android:contentDescription="Ticket icon"
                        app:tint="@android:color/white" />
                </com.google.android.material.card.MaterialCardView>

                <!-- Yellow Badge (top-right) -->
                <View
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="top|end"
                    android:layout_marginTop="10dp"
                    android:layout_marginEnd="10dp"
                    android:background="@drawable/circle_shape"
                    android:backgroundTint="#FFC107"
                    android:elevation="10dp"/>
            </FrameLayout>

            <!-- Title -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="EventMaster"
                android:textSize="32sp"
                android:textStyle="bold"
                android:textColor="#000000"
                android:layout_marginBottom="8dp" />

            <!-- Tagline -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Your fair shot at every event"
                android:textSize="16sp"
                android:textColor="#6B7280"
                android:layout_marginBottom="60dp" />

            <!-- Progress Indicators (Three dots) -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center">

                <!-- Active dot (darker teal) -->
                <View
                    android:layout_width="10dp"
                    android:layout_height="10dp"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/circle_shape"
                    android:backgroundTint="#5FB3AD"/>

                <!-- Inactive dots (lighter teal) -->
                <View
                    android:layout_width="10dp"
                    android:layout_height="10dp"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/circle_shape"
                    android:backgroundTint="#B2DFDB"
                    android:alpha="0.7"/>

                <View
                    android:layout_width="10dp"
                    android:layout_height="10dp"
                    android:background="@drawable/circle_shape"
                    android:backgroundTint="#B2DFDB"
                    android:alpha="0.7"/>
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

    <!-- Bottom Section (Fixed at bottom) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="32dp"
        android:paddingTop="0dp">

        <!-- Get Started Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnContinue"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Get Started"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:drawableEnd="@drawable/ic_arrow_right"
            android:drawablePadding="8dp"
            app:cornerRadius="16dp"
            app:backgroundTint="#5FB3AD"
            app:elevation="4dp"
            android:layout_marginBottom="16dp" />

        <!-- Footer -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="Fair • Transparent • Equal chances"
            android:textSize="12sp"
            android:textColor="#6B7280"
            android:gravity="center" />

    </LinearLayout>

</FrameLayout>
