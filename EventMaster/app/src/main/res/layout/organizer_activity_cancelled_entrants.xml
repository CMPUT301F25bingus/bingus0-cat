<?xml version="1.0" encoding="utf-8"?>
<!-- Selected Entrants Activity Layout - Matches Figma Design -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#80CBC4">

    <!-- Back Button Header -->
    <LinearLayout
        android:id="@+id/back_button_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical"
        android:background="#B2DFDB"
        android:clickable="true"
        android:focusable="true">

        <ImageView
            android:id="@+id/back_arrow"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_revert"
            android:contentDescription="Back arrow"
            app:tint="@android:color/black" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Back to View Entrants"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black" />

    </LinearLayout>

    <!-- cancelled Entrants Title -->
    <TextView
        android:id="@+id/cancelledEntrantsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="16dp"
        android:text="Cancelled entrants"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:background="@drawable/title_background"
        android:padding="12dp" />

    <!-- RecyclerView for Entrants List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/cancelled_entrants_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false"
        android:scrollbars="vertical" />

    <!-- Bottom Actions Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="#80CBC4">

        <!-- Action Links -->
        <TextView
            android:id="@+id/view_enrolled_link"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:text="click here to view a list of all entrants who have enrolled"
            android:textSize="12sp"
            android:textColor="#1976D2"
            android:textStyle="italic"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:id="@+id/cancel_entrants_link"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="cancel the entrants who haven't yet signed up for the events"
            android:textSize="12sp"
            android:textColor="#D32F2F"
            android:textStyle="italic"
            android:clickable="true"
            android:focusable="true" />

        <!-- Total Count -->
        <TextView
            android:id="@+id/total_selected_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="Total selected entrants: 3"
            android:textSize="14sp"
            android:textColor="@android:color/black" />

        <!-- Action Buttons Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="2">

            <!-- Send Notification Button -->
            <TextView
                android:id="@+id/send_notification_button"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:text="Send Notification 📧"
                android:textSize="14sp"
                android:textColor="@android:color/black"
                android:textStyle="italic"
                android:gravity="start"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground"
                android:padding="8dp" />

            <!-- Export CSV Button -->
            <TextView
                android:id="@+id/export_csv_button"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="Export CSV"
                android:textSize="14sp"
                android:textColor="@android:color/black"
                android:textStyle="italic"
                android:gravity="end"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground"
                android:padding="8dp" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>

