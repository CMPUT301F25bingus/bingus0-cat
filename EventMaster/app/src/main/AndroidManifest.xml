<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission for QR code scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventMaster">
        <activity
            android:name=".ui.organizer.OrganizerHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.organizer.OrganizerManageEventsActivity" />
        <activity
            android:name=".ui.organizer.OrganizerCreateEventActivity"
            android:exported="false"
            android:label="Create Event" />
        <activity
            android:name=".ui.entrant.EventDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.entrant.EventListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.admin.AdminEventListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.qr.QRScannerActivity"
            android:screenOrientation="portrait"
            android:exported="false" />
    </application>

</manifest>